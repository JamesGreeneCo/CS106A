import acm.program.*;

public class pythagoreanTheorem extends ConsoleProgram {
	
	private static final long serialVersionUID = 1L;
	
	public void run() {
		sayWelcomeMessage();
		askUserInput();
	}
	
	private void sayWelcomeMessage() {
		//println("Enter values to compute the Pythagorean Theorem");
	}
	private void askUserInput() {
		/*
		int a = readInt ("a:");	//asks user to enter an integer of a
		int b = readInt ("b:");	//asks user to enter an integer of b
		double x = (double)a;	//converts variable 'a' from an integer to a double
		//double y = (double)b;	//converts variables 'b' from an interger to a double
		double c = Math.sqrt((x*x) + (b*b));	//calculates square root
		println("c:"+ c);	//displays value as a double
		*/
	}
}